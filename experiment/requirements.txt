# Tested on Python 3.13
numpy==2.4.3
pandas==2.3.3
matplotlib==3.10.8
scipy==1.17.1
opencv-python==4.13.0
requests==2.32.3
statsmodels==0.14.6      # PGLS / phylogenetic regression
adjustText==1.3.0        # label de-collision in artsy_fig1.py

# segmentation
rembg==2.0.76            # pulls onnxruntime; downloads u2netp + isnet-general-use on first run

# the "is this actually a fish?" classifier used when picking cut-outs.
# torchvision must match torch: 0.22.x pairs with torch 2.7.x.
torch==2.7.1
torchvision==0.22.1
